%% ULYSSES %%
% Date: 30/01/2022

%% Linear Constraints %% 
% Function to compute the residual vector of the linear constraints of the problem

% Inputs: - vector params, the vector of parameters and problem data
   
function [A, b, Aeq, beq] = LinConstraints(obj, params)
    % Linear inequalities
    A = []; 
    b = [];
    
    % Linear constraints
    Aeq = []; 
    beq = [];
end